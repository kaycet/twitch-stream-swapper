{
  "setupScript": "echo Setup complete - Chrome extension requires no build step",
  "devServerScript": "node scripts/dev-server.js",
  "cleanupScript": "",
  "copyFiles": [
    "scripts"
  ],
  "ignorePatterns": [
    "node_modules",
    ".git",
    "dist",
    "*.log"
  ]
}

